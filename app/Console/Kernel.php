<?php

use Illuminate\Console\Scheduling\Schedule;

return function (Schedule $schedule) {

//    $schedule->command('reminders:created')
//        ->everyMinute()
//        ->appendOutputTo(storage_path('logs/reminders.log'));
//
//    $schedule->command('reminders:upcoming')
//        ->hourly()
//        ->appendOutputTo(storage_path('logs/reminders.log'));
//
//    $schedule->command('tattoo:generate-prompts')
//        ->everyMinute()
//        ->appendOutputTo(storage_path('logs/prompts.log'));
//
//    $schedule->command('tattoo:generate-images')
//        ->everyMinute()
//        ->appendOutputTo(storage_path('logs/prompts.log'));
//
//    // Example: Clear cache daily at midnight
//    $schedule->command('cache:clear')->dailyAt('00:00');


    // * * * * * php public_html/artisan schedule:run >> /dev/null 2>&1
};
